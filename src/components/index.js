import Features from "./Features";
import Download from "./Download";
import { SectionWrapper, SectionWrapper2, SectionWrapper3, SectionWrapper4} from "./SectionWrapper";



export  {
    Features, Download, SectionWrapper, SectionWrapper2, SectionWrapper3, SectionWrapper4
}